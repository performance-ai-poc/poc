fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Read-only HTTP client for the OpenObserve query API. Also required by
# fastapi.testclient.TestClient, so it is a hard dependency.
httpx==0.28.1

# Test-only.
pytest==9.1.1
