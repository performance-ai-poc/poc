# Agent 2 Service

Reserved for a future agent service in the AI chat POC.

This folder is currently empty. If this agent works with tenant-specific data,
make the authorization and `tenant_id` rules explicit before wiring it to the
orchestrator. The current orchestrator accepts caller-supplied `tenant_id`
values without authentication.

When implemented, document the local run command, tests, Docker image, API or
queue contract, and emitted telemetry.
