{{- if and (.Values.dashboardUi.enabled | default true) .Values.dashboardUi.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "observability.fullname" . }}-dashboard-ui
  labels:
    app.kubernetes.io/name: dashboard-ui
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  {{- if .Values.dashboardUi.ingress.className }}
  ingressClassName: {{ .Values.dashboardUi.ingress.className | quote }}
  {{- end }}
  rules:
    - host: {{ .Values.dashboardUi.ingress.host | quote }}
      http:
        paths:
          - path: {{ .Values.dashboardUi.ingress.path | quote }}
            pathType: {{ .Values.dashboardUi.ingress.pathType | quote }}
            backend:
              service:
                name: {{ include "observability.fullname" . }}-dashboard-ui
                port:
                  number: {{ .Values.dashboardUi.service.port }}
  {{- if and .Values.dashboardUi.ingress.tls.enabled .Values.dashboardUi.ingress.tls.secretName }}
  tls:
    - hosts:
        - {{ .Values.dashboardUi.ingress.host | quote }}
      secretName: {{ .Values.dashboardUi.ingress.tls.secretName | quote }}
  {{- end }}
{{- end }}
