# Dashboard UI Public Assets

Static files in this folder are served directly by Vite in development and by
Nginx in the production container.

Current files:

- `favicon.svg` - browser icon.
- `icons.svg` - SVG symbol sprite used by the starter UI.

Use this folder for stable public URLs. Imported React assets should usually
live under `src/assets/`.
